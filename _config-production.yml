# In order to build for production run:
# $ jekyll build --config _config.yml, _config-production.yml
#
#
# URL of static site
url: "https://production.platforms.linaro.org"
# Destination folder
destination: production.platforms.linaro.org
# Production boolean used in layouts / includes
production: true